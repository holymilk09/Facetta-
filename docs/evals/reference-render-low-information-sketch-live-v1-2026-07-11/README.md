# Low-information sketch live evaluation

This case is a deterministic synthetic fixture, not founder, customer, or
designer artwork. `source.png` intentionally provides only:

- one uneven double-line shank;
- one green oval center;
- four claw marks;
- three small circles per shoulder; and
- short material/component notes with no dimensions or hidden construction.

The canonical `REFERENCE_RENDER` operation returned `candidate.jpg` in one
Grok attempt (18.92 seconds), with a 92 comparative QA score. Every automated
source-fidelity check passed; the only warning was the invariant that a
creative raster is not factory authority.

Human review confirms the returned image preserves the green oval, four center
claws, exactly three clear shoulder stones per side, and a simple polished
yellow-gold band. The camera is closer to an elevated front view than a strong
three-quarter view. The model necessarily proposed a minimal gallery/seat that
was absent from the sketch; that construction remains review-only and must not
enter a factory sheet or immutable specification without designer confirmation.

No product asset, specification, measurement, approval, or factory record was
created by this evaluation.
