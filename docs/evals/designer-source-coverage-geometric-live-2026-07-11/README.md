# Geometric emerald source-coverage audit (superseded diagnostic)

The source plate read succeeded, but the independent audit returned a
contradictory bidirectional mapping (`blind.004`). The older harness originally
reported that as `read_error`; this retained artifact is normalized to
`coverage_status: invalid` so aggregate gates remain truthful. No project or
product asset was created. See the v2 run for current evidence.
