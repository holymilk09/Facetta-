{
  "create": {
    "lane": "fast_visual",
    "execution_mode": "candidate_job",
    "review_authority": "candidate_decision",
    "credits_per_output": 15,
    "input_requirements": ["brief_or_reference"],
    "context_requirements": [],
    "output_type": "design_revision",
    "authority": "design_record",
    "ui_schema": [
      {"id": "brief", "label": "Design brief", "kind": "text", "required": false},
      {"id": "master", "label": "Master geometry", "kind": "reference", "required": false, "reference_role": "master_geometry"},
      {"id": "style", "label": "Material or style", "kind": "reference", "required": false, "reference_role": "material_style"},
      {"id": "construction", "label": "Construction detail", "kind": "reference", "required": false, "reference_role": "construction_detail"},
      {"id": "brand", "label": "Brand direction", "kind": "reference", "required": false, "reference_role": "brand_direction"}
    ]
  },
  "vary": {
    "lane": "instant",
    "execution_mode": "instant_transaction",
    "review_authority": "none",
    "credits_per_output": 0,
    "input_requirements": ["direction"],
    "context_requirements": ["active_project", "active_revision"],
    "output_type": "variation_set",
    "authority": "design_record",
    "ui_schema": [
      {"id": "direction", "label": "Variation name", "kind": "text", "required": true}
    ]
  },
  "refine": {
    "lane": "trusted_structural",
    "execution_mode": "candidate_job",
    "review_authority": "candidate_decision",
    "credits_per_output": 20,
    "input_requirements": ["instruction"],
    "context_requirements": ["active_project", "active_revision"],
    "output_type": "design_revision",
    "authority": "design_record",
    "ui_schema": [
      {"id": "instruction", "label": "Change instruction", "kind": "text", "required": true},
      {"id": "mask", "label": "Target region", "kind": "reference", "required": false, "reference_role": "edit_mask"}
    ]
  },
  "views": {
    "lane": "fast_visual",
    "execution_mode": "candidate_job",
    "review_authority": "candidate_decision",
    "credits_per_output": 15,
    "input_requirements": ["view_set"],
    "context_requirements": ["active_project", "active_revision", "exact_specification"],
    "output_type": "view_set",
    "authority": "visual_preview",
    "ui_schema": [
      {"id": "view_set", "label": "Views", "kind": "select", "required": true}
    ]
  },
  "present": {
    "lane": "fast_visual",
    "execution_mode": "candidate_job",
    "review_authority": "candidate_decision",
    "credits_per_output": 18,
    "input_requirements": ["destination"],
    "context_requirements": ["active_project", "active_revision"],
    "output_type": "presentation_pack",
    "authority": "visual_preview",
    "ui_schema": [
      {"id": "destination", "label": "Presentation destination", "kind": "select", "required": true},
      {"id": "direction", "label": "Optional art direction", "kind": "text", "required": false}
    ]
  },
  "factory": {
    "lane": "trusted_structural",
    "execution_mode": "terminal_job",
    "review_authority": "generic_transition",
    "credits_per_output": 28,
    "input_requirements": [],
    "context_requirements": ["active_project", "active_revision", "exact_specification", "factory_eligible"],
    "output_type": "factory_review_pack",
    "authority": "production_review",
    "ui_schema": []
  }
}
